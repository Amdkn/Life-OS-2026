---
title: LD07 IT SDD - Agent Harness Integration
sector: IT
priority: 2
type: SDD
status: draft
created_at: 2026-03-18T12:00:00Z
author: GravityClaw (A'"0)
---
# LD07 IT - "Everything-Claude-Code" Assimilation (Cyborg Engine)

## Target Layer: Meta-Tools / L0 Infrastructure
## Purpose: Massively upgrade the cognitive and procedural capabilities of Antigravity, Conductor, and Ralph Loops by assimilating the best open-source AI performance optimizations.

### Core Discovery Analysis
The repository is an optimization system for AI IDEs containing:
1. **25 Sub-Agents**: Pre-prompted personas (`architect.md`, `code-reviewer.md`, `security-reviewer.md`) for specialized delegation.
2. **108 Skills**: Granular workflow blueprints ranging from `tdd-workflow` to `continuous-learning-v2` and `agent-harness-construction`.
3. **JS Hooks**: Robust Node.js scripts triggering on Pre/Post tool execution (like Plankton for code formatting or AgentShield for security).

### Sub-Sector Execution (Kang Dynasty Squad)
- [ ] **Kang (Repo Control)**: Migrate the core universal rules (`rules/common`) into `C:\Users\amado\.agent\rules\` to globally standardize the AI's coding behavior.
- [ ] **Iron Lad (New Capabilities)**: Wire the `hooks/hooks.json` logic into Gemini CLI/Antigravity to enforce silent write-time code quality via Plankton, eliminating endless syntax correction loops.
- [ ] **Immortus (Legacy Patterns)**: Filter and ingest structural skills (`skills/backend-patterns/`, `skills/testing/`) into `C:\Users\amado\.agent\skills\`.
- [ ] **Rama-Tut (Access / Delegation)**: Map the 25 sub-agents into the `.gemini/agents/` folder so Conductor (A2) can explicitly summon them (e.g., `gemini --agent code-reviewer`).

### 🛑 Veto & Alignment Review (Commanditaire)
Avant de finaliser le PRD (Phase A1) et l'ADR (Phase A2), l'A0 a besoin de directives claires sur la portée de l'ingestion :
1. **Scope d'Ingestion** : Souhaites-tu que l'on copie *TOUTES* les 108 skills en masse dans ton écosystème, ou devons-nous faire une sélection granulaire (cherry-picking) uniquement sur tes stacks actuelles (Node.js, PowerShell, etc.) ?
2. **Niveau d'Intrusion des Hooks** : Es-tu d'accord pour que l'on modifie le fichier de configuration principal de ton IDE Antigravity (`settings.json` / `hooks.json`) pour qu'il lance automatiquement les scripts Node.js de l'auteur en arrière-plan à chaque fois que je modifie un fichier (Plankton / Formatters) ?
